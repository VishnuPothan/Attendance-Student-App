<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.group2.attendancestudentapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_calendar"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_calendar"
        android:supportsRtl="true"
        android:theme="@style/Theme.AttendanceStudentApp">
        <activity android:name=".ProfileEdit"></activity>
        <activity android:name=".MessageSentActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".OTPVerificationActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>